

// warning: this is a wrong file, included to webpack works
// it's actually bad typescript, but webpack fails if we don't have this, because it has a bug and doesn't look in the d.ts file

import "./2sxc-js/main";